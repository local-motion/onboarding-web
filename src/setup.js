// This module contains the global settings for our application. These can be imported throughout the application.

import {createBrowserHistory} from "history";

export const history = createBrowserHistory();

